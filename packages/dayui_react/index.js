import '@xujunhao2010/dayui/dist/dayui.css'
import React from 'react';
import {createComponent} from '@lit/react';
import {
    Icon,
    Button,
    IconButton,
    ButtonGroup,
    Menu,
    MenuItem,
    MenuDivider,
    Dropdown,
    Input,
    Select,
    Checkbox,
    Switch,
    Radio,
    RadioGroup,
    Divider,
    Fab,
    Card,
    CardPart,
    CardButton,
    Avatar,
    AvatarText,
    Chip,
    Tabs,
    Tab,
    TabPanel,
    List,
    ListItem,
    Slider
} from "@xujunhao2010/dayui/index.js";

const DayIcon = createComponent({
    tagName: 'day-icon', elementClass: Icon, react: React
});
const DayButton = createComponent({
    tagName: 'day-button', elementClass: Button, react: React
});
const DayButtonIcon = createComponent({
    tagName: 'day-button-icon', elementClass: IconButton, react: React
});
const DayButtonGroup = createComponent({
    tagName: 'day-button-group', elementClass: ButtonGroup, react: React
});
const DayMenu = createComponent({
    tagName: 'day-menu', elementClass: Menu, react: React
});
const DayMenuItem = createComponent({
    tagName: 'day-menu-item', elementClass: MenuItem, react: React
});
const DayMenuDivider = createComponent({
    tagName: 'day-menu-divider', elementClass: MenuDivider, react: React
});
const DayDropdown = createComponent({
    tagName: 'day-dropdown', elementClass: Dropdown, react: React
});
const DayInput = createComponent({
    tagName: 'day-input', elementClass: Input, react: React
});
const DaySelect = createComponent({
    tagName: 'day-select', elementClass: Select, react: React
});
const DayCheckbox = createComponent({
    tagName: 'day-checkbox', elementClass: Checkbox, react: React
});
const DaySwitch = createComponent({
    tagName: 'day-switch', elementClass: Switch, react: React
});
const DayRadio = createComponent({
    tagName: 'day-radio', elementClass: Radio, react: React
});
const DayRadioGroup = createComponent({
    tagName: 'day-radio-group', elementClass: RadioGroup, react: React
});
const DayDivider = createComponent({
    tagName: 'day-divider', elementClass: Divider, react: React
})
const DayFab = createComponent({
    tagName: 'day-fab', elementClass: Fab, react: React
})
const DayCard = createComponent({
    tagName: 'day-card', elementClass: Card, react: React
})
const DayCardPart = createComponent({
    tagName: 'day-card-part', elementClass: CardPart, react: React
})
const DayCardButton = createComponent({
    tagName: 'day-card-button', elementClass: CardButton, react: React
})
const DayAvatar = createComponent({
    tagName: 'day-avatar', elementClass: Avatar, react: React
})
const DayAvatarText = createComponent({
    tagName: 'day-avatar-text', elementClass: AvatarText, react: React
})
const DayChip = createComponent({
    tagName: 'day-chip', elementClass: Chip, react: React
})
const DayTabs = createComponent({
    tagName: 'day-tabs', elementClass: Tabs, react: React
})
const DayTab = createComponent({
    tagName: 'day-tab', elementClass: Tab, react: React
})
const DayTabPanel = createComponent({
    tagName: 'day-tab-panel', elementClass: TabPanel, react: React
})
const DayList = createComponent({
    tagName: 'day-list', elementClass: List, react: React
})
const DayListItem = createComponent({
    tagName: 'day-list-item', elementClass: ListItem, react: React
})
const DaySlider = createComponent({
    tagName: 'day-slider', elementClass: Slider, react: React
})
export {
    DayIcon,
    DayButton,
    DayButtonIcon,
    DayButtonGroup,
    DayMenu,
    DayMenuItem,
    DayMenuDivider,
    DayDropdown,
    DayInput,
    DaySelect,
    DayCheckbox,
    DaySwitch,
    DayRadio,
    DayRadioGroup,
    DayDivider,
    DayFab,
    DayCard,
    DayCardPart,
    DayCardButton,
    DayAvatar,
    DayAvatarText,
    DayChip,
    DayTabs,
    DayTab,
    DayTabPanel,
    DayList,
    DayListItem,
    DaySlider
};